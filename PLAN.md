# ECAD — план розробки

Оновлено: 2026-09-10. План враховує уточнення користувача та п'ять наданих PDF.

## Підтверджені вимоги

- Windows і Linux (Ubuntu) — основні платформи. Користувач підтвердив Ubuntu; версію ще не вказано, початковий орієнтир для перевірки — 24.04 LTS. Веб та мобільні пристрої — можливий наступний розвиток.
- Малювання ліній і фігур, групування, з'єднання, сітка, прив'язка до сітки.
- Уніфікована система розмірів для всіх геометричних фігур: кожну фігуру можна створити довільно мишею або точно за числовими параметрами, а потім редагувати через ті самі параметри.
- Можливість бібліотек компонентів IEC та ANSI.
- Імпорт старих форматів не потрібен. PDF використовуються як еталони функціоналу, а не як завдання на автоматичну конвертацію.
- Український інтерфейс прийнято як робоче припущення за мовою спілкування.

## Архітектура

C# / .NET 10 / Avalonia. Початкова WPF-пропозиція скасована через вимогу Linux.

Avalonia підтримує Windows, Linux, Android, iOS та WebAssembly; веб і мобільна версія потребуватимуть окремої адаптації збереження, друку, навігації та сенсорного вводу. Наявність підтримки у фреймворку не означає готовності цих версій ECAD.

- ECAD.Core: геометрія у мм, модель документа, історія змін, формат файлів. Без залежності від UI.
- ECAD.Desktop: вікно та власне векторне полотно Avalonia. Рендерер читає модель.
- ECAD.Checks: виконувані автоматичні перевірки моделі, файлів і вводу через Avalonia.Headless.
- Надалі: платформонезалежна модель контактів/кіл, бібліотек, сторінок і експорт; платформні адаптери для файлів/друку.

Модель зараз містить один аркуш; багатосторінковість буде окремим розширенням зі зміною версії формату. Координати геометрії не залежать від масштабу екрана. Реальні габарити пристрою будуть окремі від умовного символу.

## Правила редагування

1. Кожна фігура має два рівноправні способи побудови: опорні точки задаються мишею або після першої точки вводяться точні числові параметри. Перемикати окремий режим «точне креслення» не потрібно.
2. Числове значення має пріоритет над сіткою. При кроці 2,5 мм введені 12,3 мм не округляються до 12,5 мм. Опорну точку можна прив'язати; решта геометрії обчислюється з введених параметрів.
3. Параметри вводяться у спільній плаваючій панелі біля курсора: набір числа активує перше поле, Tab переходить між полями, Enter завершує побудову, Escape скасовує її та повертає режим вибору. Крапка й кома підтримуються як десятковий роздільник.
4. Порожнє поле залишає відповідний параметр вільним і дозволяє визначити його курсором. Недопустимі значення не створюють команду і пояснюються в інтерфейсі.
5. Існуюча фігура редагується через ту саму панель параметрів. Зміна значення є однією операцією Undo/Redo; опорна точка або сторона, яка залишається нерухомою, має бути передбачуваною та видимою.
6. Групування об'єднує редагування геометрії, але не створює електричний зв'язок. Прототип має плоскі групи; вкладені групи поки не підтримуються.
7. Переміщення групи зберігає взаємне положення елементів. Одна завершена дія — один крок Undo.
8. Розміри асоціативні: дві точки, точка–лінія, паралельні лінії, сторона прямокутника, радіус і діаметр кола; посилання на геометрію зберігаються у файлі. На наступному етапі розмір стає керувальним: редагування його числа змінює пов'язану геометрію.
9. Контакт/явний вузол має пріоритет над сіткою у заданому екранному допуску; звичайний перетин провідників не створює з'єднання.
10. При суперечності числових параметрів, керувального розміру та прив'язки програма не змінює геометрію непомітно. Вона показує конфлікт і залишає попередній коректний стан.

## Етапи

До готовності програми релізи нумеруються послідовно: 0.10, 0.11, 0.12 і далі. 1.0 виходить після завершення основного робочого циклу створення, перевірки та випуску схем.

| Етап | Результат та приймання | Стан |
| --- | --- | --- |
| 0. Приклади й вимоги | Аналіз 20 сторінок, платформи та пріоритетні функції | Виконано; див. docs/REFERENCE-REVIEW.md |
| 1. Геометричний прототип | A3, zoom/pan, сітка, прив'язка, лінія/прямокутник/коло, точна довжина, вибір, переміщення, групи, вільний розмір, Undo/Redo, save/open | Реалізовано; потрібна ручна перевірка користувачем |
| 2. Уніфікована геометрія та розміри | Довільне й числове створення всіх фігур, спільна панель параметрів, редагування наявної геометрії, керувальні асоціативні розміри, дуги, полілінії й операції над сегментами | Виконано у 0.12 для запланованого набору базової геометрії |
| 3. Символи й з'єднання | Визначення символів, контакти, редактор символів, стартовий набір, ортогональні провідники, явні вузли | Частково: окремі текстові позначення, власні символи, 12 IEC/ANSI-чернеток, ERC, провідники, кола й вузли готові у 0.7.2 |
| 4. Прикладні схеми | Аркуші A4/A3, рамки/штампи, зони, міжсторінкові посилання, повторні подання пристрою | Далі |
| 5. Кабелі й випуск | Кабелі та жили, нумерація, кабельний журнал, BOM, PDF/друк, резервні копії | Далі |
| 6. Пілот | Відтворення узгодженого зразка, перевірка на Windows і цільовому Linux, виправлення та пакети встановлення | Далі |
| 7. Розвиток | 2D компоновка шафи; окремі дослідження веб/мобільного інтерфейсу | Поза першим MVP |

Undo/Redo, захист відкриття і збереження включені у фундамент. Відновлення після аварії та друк мають бути готові до реального використання.

## Оновлення 0.7

Позиційне позначення символу стало звичайним текстовим елементом зі своїми координатами, висотою, поворотом і редагуванням. При русі символу текст слідує за ним, але його можна пересувати окремо. Виправлено двигун: круглий контур, `M`, `3~` і три виводи до контуру. Формат файлу — версія 6 з міграцією позначень із версії 5.

## Оновлення 0.7.1

Виправлено потенційне аварійне завершення під час створення власного символу: список бібліотеки оновлюється один раз у наступному циклі UI. Додано перехоплення помилок застосунку, UI та фонових задач, постійний файл журналу й перегляд журналу з головної панелі. Асинхронні діалоги та файлові операції показують помилку в інтерфейсі й записують її деталі.

## Оновлення 0.7.2

Виправлено шаблон ComboBox бібліотеки: під час заміни списку Avalonia може тимчасово передати порожній елемент, який раніше спричиняв `NullReferenceException` і переривав показ щойно створеного символу. Створене визначення тепер з'являється у списку та автоматично вибирається.

## Оновлення 0.9.0

Один інструмент створює електричні провідники або графічні лінії залежно від перемикача типу. Панель властивостей реагує на виділення і редагує базові параметри всіх наявних типів. Для бібліотечних символів тут будуть окремі секції опису, номіналів, фізичних габаритів, артикулу та наборів контактів.

## Дорожня карта до 1.0

| Версія | Головний результат | Критерій завершення |
| --- | --- | --- |
| 0.11 | Керувальні асоціативні розміри | Число розміру змінює пов’язану лінію, прямокутник або коло; довідковий розмір лише показує значення; конфлікт не псує геометрію |
| 0.12 | Розширене геометричне редагування | Дуга і полілінія створюються мишею та числами; вершини редагуються; додано обрізання, продовження і розбиття сегмента |
| 0.13 | Модульна модель бібліотек і пристроїв | Реалізовано: довільні бібліотеки, типи, сімейства/серії, універсальні конфігурації, варіанти, параметри, контакти, фізичні виконання та зв’язок із символом |
| 0.14 | Браузер і редактор бібліотеки | Створення й редагування бібліотек, пошук і категорії без зашитого поділу IEC/ANSI/Custom; для варіанта редагуються умовне позначення, точка вставки, контакти та один або кілька фізичних відповідників |
| 0.15 | Багатосторінковий проєкт | Сторінки A4/A3, рамка, штамп, зони, навігація і міжсторінкові посилання зберігаються в одному `.ecad` |
| 0.16 | Електрична модель проєкту | Повторні подання пристрою, нумерація провідників, клеми, кабелі та жили мають стабільні ID; ERC перевіряє сумісність контактів |
| 0.17 | 2D-компоновка щита | Символ схеми зв’язаний з фізичним виглядом; доступні DIN-рейки, монтажна панель, габарити і базова перевірка колізій |
| 0.18 | Випуск документації | PDF/друк з масштабом, BOM, перелік клем, кабельний журнал і перелік з’єднань формуються з однієї моделі |
| 0.19 | Надійність і пілот | Автозбереження, відновлення після збою, резервні копії та міграції пройшли тести; один наданий проєкт відтворено на Windows і Ubuntu |
| 1.0 | Перший робочий випуск | Повний цикл «схема → перевірка → компонування → звіти → PDF» працює на Windows і Ubuntu без блокуючих помилок |

Веб і мобільні клієнти починаються після 1.0. Формат файлу і `ECAD.Core` вже проєктуються без залежності від desktop UI.

## Реалізований етап: 0.13

- Каталог має ієрархію бібліотека → тип пристрою → сімейство/серія → варіант → фізичне виконання; усі сутності мають стабільні ID.
- Тип задає довільні поля конфігурації. Полюси `1P/2P/3P/4P`, кількість каналів, склад контактів, напруга котушки або інше виконання є звичайними даними, а не фіксованими типами програми.
- Варіант містить артикул, номінал, структуровані параметри, контакти, ключ умовного позначення та один або кілька фізичних відповідників із габаритами, монтажем, DIN-модулями й необов’язковим контуром.
- Встановлений символ може посилатися на сумісний варіант і фізичне виконання. Панель властивостей показує всі ці дані та дозволяє перемкнути зв’язок.
- Валідація відхиляє повторні ID, невідомі чи пропущені параметри конфігурації, недозволені значення, некоректні габарити та несумісний символ. Формат `.ecad` версії 10 читає версії 1–9.

## Попередній реалізований етап: 0.12

- Полілінія створюється послідовними кліками або точними сегментами «довжина + кут»; Enter завершує побудову.
- Дуга створюється через три точки або чисельно: центр, радіус, початковий напрям курсора та кут дуги.
- Вершини полілінії редагуються перетягуванням маркерів і точними координатами в панелі властивостей. Радіус та кути дуги редагуються там само.
- Розбиття ділить пряму на два елементи або вставляє вершину в полілінію. Обрізання і продовження працюють для прямої лінії та прямої межі.
- Асоціативні посилання переназначаються під час розбиття; кожна зміна має один крок Undo/Redo. Формат `.ecad` версії 9 читає версії 1–8.

## Найближчий етап: 0.14

Реалізувати браузер і повний редактор бібліотек поверх моделі 0.13: створення, перейменування, копіювання, пошук, категорії, редагування універсальних полів конфігурації, варіантів, контактів і фізичних відповідників без зашитого поділу IEC/ANSI/Custom.

## Реалізований етап: 0.11

У 0.11 кожен керувальний розмір має одного власника і одну передбачувану нерухому опору. Це дає поведінку Fusion 360 для основних фігур без повного геометричного solver-а.

1. Додати види розміру: вирівняний, горизонтальний, вертикальний, радіус і діаметр. Кутовий розмір — після стабілізації лінійних.
2. Додати властивість `Довідковий / Керувальний` та числове поле у панелі властивостей.
3. Визначити нерухому опору: початок лінії, вибраний кут прямокутника, центр кола. Перед застосуванням показувати попередній перегляд.
4. Оновлювати пов’язані розміри, вузли і кінці провідників однією операцією Undo/Redo.
5. Зберігати тип, опору, цільове значення і прив’язки у `.ecad` з міграцією попередніх файлів.
6. Покрити тестами зміну кожного типу фігури, копіювання, видалення, save/open і відмову при конфлікті.

### Приймання 0.11

1. Користувач ставить розмір між точками або лініями і бачить, до якої геометрії він прив’язаний.
2. Після перемикання в керувальний режим введене число перебудовує власника розміру.
3. Введений точний розмір не округляється сіткою і зберігається після повторного відкриття.
4. Undo/Redo повертає одночасно геометрію і всі залежні розміри.
5. Недопустиме або суперечливе значення показує зрозумілу помилку і не змінює документ.

## Реалізована система побудови 0.8

У релізах 0.8–0.8.2 було закладено уніфіковану систему побудови та розмірів. Лінія є еталоном взаємодії: перша точка → довільна кінцева точка або точні параметри → Enter. Той самий принцип поширюється на всі фігури.

### 0.8.0 — спільне ядро числового вводу

Реалізовано у версії 0.8 для лінії, прямокутника та кола.

- Одна модель параметрів і одна плаваюча панель замість окремої логіки кожного інструмента.
- Єдина обробка Tab, Shift+Tab, Enter, Escape, десяткової крапки й коми.
- Попередній перегляд одночасно реагує на курсор і вже зафіксовані числові поля.
- Частково заповнені параметри допустимі: зафіксовані значення зберігаються, решта визначається курсором.
- Перевірка діапазонів, нульових і від'ємних значень без створення пошкодженої геометрії.

### 0.8.1 — параметри фігур

Лінія, прямокутник і коло реалізовані у версії 0.8. Точні сегменти провідника реалізовані у 0.8.1, довільні кути та один циклічний перемикач кроку 15°/30°/45° — у 0.8.2. Дуга й полілінія залишаються наступними підетапами після керувальних розмірів.

| Фігура | Довільна побудова | Побудова за розмірами |
| --- | --- | --- |
| Лінія | початок → кінцева точка | довжина + кут |
| Прямокутник | перший кут → протилежний кут | ширина + висота |
| Коло | центр → точка на колі | радіус або діаметр |
| Провідник | послідовність точок; вільна ортогональна траса або кутова прив’язка 15°/30°/45° | довжина поточного сегмента + довільний кут |
| Дуга | початок → проміжна точка → кінець | радіус + початковий і кінцевий кути |
| Полілінія | послідовність довільних сегментів | довжина + кут для кожного сегмента |

Прямокутник на першій реалізації залишається ортогональним до аркуша; поворот виконується після створення. Для кола користувач перемикає радіус/діаметр без зміни геометрії. Дуги й полілінії додаються після стабілізації поточних фігур на тому самому ядрі.

### 0.8.2 — редагування та керувальні розміри

- Подвійний клік по фігурі відкриває її параметри в тій самій плаваючій панелі.
- Для прямокутника редагуються ширина і висота; для кола — радіус або діаметр; для лінії — довжина і кут.
- При редагуванні явно показується нерухома опора: початкова точка лінії, вибраний кут прямокутника або центр кола.
- Розмір можна переключити між довідковим і керувальним. Зміна керувального числа перебудовує пов'язану фігуру.
- Підтримуються лінійні, горизонтальні, вертикальні, радіальні та діаметральні розміри. Кутові розміри виконуються після них.
- Прив'язки, видалення, копіювання, групування, збереження та Undo/Redo оновлюють геометрію і розміри атомарно.

### Приймання 0.8

1. Лінію, прямокутник і коло можна створити повністю мишею, повністю числами або змішаним способом.
2. Після створення їхні параметри можна змінити без видалення й повторного креслення.
3. Числові значення не округляються сіткою та зберігаються після save/open.
4. Асоціативний розмір слідує за геометрією; керувальний розмір може змінити геометрію.
5. Кожна завершена побудова або зміна є одним кроком Undo/Redo.
6. Поведінка клавіш і панелі однакова для всіх інструментів.

Після приймання 0.8: глобальна бібліотека та повний редактор символів, потім аркуші A4/A3 з рамкою і штампом, сторінки та міжсторінкові посилання. Поглиблення властивостей контактів і пристроїв виконується перед відтворенням реального фрагмента «частотник.pdf».

З'єднання перевіряти окремими сценаріями: перетин без вузла; T-з'єднання з вузлом; переміщення символу; обертання контактів; Undo після групової операції; save/open зі збереженням зв'язків. Не обчислювати фізичну довжину кабелю за довжиною лінії на принциповій схемі.

## Модульні бібліотеки пристроїв

IEC та ANSI будуть стартовими бібліотеками, а не зашитими режимами програми. Користувач зможе створювати, перейменовувати, копіювати, імпортувати й експортувати довільні бібліотеки. Ієрархія даних: бібліотека → тип пристрою → сімейство/серія → варіант → фізичний відповідник.

Тип пристрою визначає довільні поля конфігурації. Для автоматичного вимикача це може бути кількість полюсів зі значеннями 1P, 2P, 3P, 4P або іншим переліком; для іншого приладу — кількість каналів, виконання контактів, напруга котушки, спосіб керування чи монтажу. Поле може мати дозволений список або приймати довільний текст, тому додавання нового варіанта не потребує зміни коду. Кожен варіант містить виробника ABB, Schneider Electric або іншого, серію, артикул, довільний текст номіналу та структуровані електричні параметри. Варіант зв’язується з умовним позначенням на схемі, набором контактів і одним або кількома фізичними виконаннями з шириною, висотою, глибиною, способом монтажу та контуром для компонування щита. Спільні дані успадковуються від типу або сімейства, а варіант перевизначає лише відмінності.

Специфікація початкових символів і джерела стандартів — docs/SYMBOL-LIBRARY.md. Модель даних запланована на 0.13, браузер і редактор — на 0.14, використання фізичних відповідників у компонуванні — на 0.17. Відповідність стандартам позначатиметься лише після перевірки конкретних символів.

## Середовище та перевірка

Локально встановлено SDK 10.0.400-preview.0.26322.102. Залежності Avalonia зафіксовано на 12.1.2. Стабільний SDK бажаний для випуску; поточний дозволив зібрати прототип. Немає потреби встановлювати додаткову IDE.

Перевірки запускаються командою dotnet run --project tests/ECAD.Checks/ECAD.Checks.csproj. Усі 81 перевірка пройшла на Windows, включно з універсальними конфігураціями пристроїв, фізичними виконаннями, дугами, полілініями, операціями над сегментами, керувальними розмірами, панеллю властивостей, ERC та headless-вводом. Розпаковані збірки 0.13 для win-x64 та linux-x64 потребують .NET 10 Runtime. Фактичний запуск на Linux ще не перевірений.

## Що ще потрібно від користувача

Для продовження креслярського ядра додаткові матеріали не потрібні. Для перевірки Ubuntu — точна версія та можливість запустити тестову збірку. До випуску схем — бажані рамка/штамп і правила нумерації. Наявні приклади достатні для формування початкового переліку символів.

Оцінку строків наступних етапів уточнювати за результатами прототипу і реального сценарію; строки з початкового опису не вважати обіцянкою.

## Джерела

- [Платформи Avalonia](https://docs.avaloniaui.net/docs/supported-platforms)
- [Власне малювання у Avalonia](https://docs.avaloniaui.net/docs/custom-controls/custom-drawn-controls)
- [Headless-перевірки Avalonia](https://docs.avaloniaui.net/docs/testing/setting-up-the-headless-platform)
- [Avalonia.Desktop 12.1.2](https://www.nuget.org/packages/Avalonia.Desktop/12.1.2)
